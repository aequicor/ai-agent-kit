# {{TASK_SLUG}} — {{PROJECT_NAME}}

> v6.1 task state. @Main writes here after every significant step.
> Read by @Main when active_task in .planning/CURRENT.md matches this file's name.
> step_commits[] is the source of truth for per-step DONE history (replaces v6.0 verbose Timeline).

## Task

**Type:** FEATURE | BUG | TECH
**Module:**
**Description:**
**start_commit:** <sha or (no-git)>
**status:** active | SLEEP_BLOCKED

---

## Active Stage

*(fill in after CLASSIFY & CLARIFY: e.g. ANALYSIS / PLAN / EXECUTE step N / RECONCILE / TRACE / DoD / DIFF-REVIEW / CLOSE)*

---

## Step state (v6.1)

**current_step_idx:** 0     <!-- index of the step that just completed machine-checks; 0 = no step done yet; -->
                            <!-- updated by @Main at 5.6 CHECKPOINT after step N's @Reviewer CLEAN + commit. -->

### step_commits

<!-- @Main appends one entry per step at 5.4b COMMIT.
     Format (YAML inside an HTML comment so render-time parsers ignore it,
     but @Main reads via line-grep):

- step: 1
  sha: abc123de
  goal: <step.Goal first line>
  changed_files:
    - src/foo/Bar.kt
    - src/foo/BarTest.kt
  superseded: false              # set true if /kit-defect re-opens this step;
                                  # 5.10 diff-review then uses next sha for this step.
  runbook_anchor: plan.md#step-1-runbook  # link to runbook block in plan.md (or inline below)

- step: 2
  ...
-->

(empty until first 5.4b COMMIT)

---

## Last-checkpoint

<!-- Single line, replaced (NOT appended) by @Main at every checkpoint.
     Format: <ISO timestamp> — NEXT: <one-line> | BLOCKED: <one-line if blocked>
     Historical step DONE-facts live in step_commits[] above; do not duplicate here. -->

{{ISO_TIMESTAMP_PLACEHOLDER}} — NEXT: CLASSIFY & CLARIFY
