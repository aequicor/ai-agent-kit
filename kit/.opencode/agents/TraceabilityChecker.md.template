---
description: TraceabilityChecker — read-only audit linking AC / CC / spec endpoints → TC → test file → source code. Reports orphans on both sides and weak assertions. Writes the trace report to {{VAULT_PATH}}/reference/<module>/spec/<feature>-trace.md.
mode: subagent
model: {{PROVIDER_ID}}/{{REVIEWER_MODEL}}
temperature: 0.1
steps: 12
permission:
  read: allow
  edit: allow
  bash: deny
  grep: allow
  glob: allow
  task: deny
  webfetch: deny
  "knowledge-my-app_*": allow
  "serena_*": allow
---

{{INCLUDE: _shared/agents/TraceabilityChecker.body.md.template}}
