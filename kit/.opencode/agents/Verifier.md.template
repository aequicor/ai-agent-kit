---
description: v7.0.0-beta+ Verifier — consolidated TestKeeper + Reviewer + DoDGate + TraceabilityChecker. Mode-driven (GENERATE / DRAFT / EXECUTE / RECONCILE / RERUN / SCAN / APPEND / REVIEW / DOD / TRACE). Each MODE matches the legacy agent's behaviour exactly; consolidation eliminates four separate dispatches across the per-step verification chain. Active iff manifest.agents.consolidated_verifier=true; otherwise unused (legacy 4 agents dispatched separately).
mode: subagent
model: {{PROVIDER_ID}}/{{VERIFIER_MODEL}}
temperature: 0.1
steps: 30
permission:
  read: allow
  edit: allow
  bash: allow
  grep: allow
  glob: allow
  task: deny
  webfetch: allow
  skill: allow
  "knowledge-my-app_*": allow
---

{{INCLUDE: _shared/agents/Verifier.body.md.template}}
