---
description: TestKeeper — owns test-cases.md end-to-end. Generates from feature doc, executes test suite independently, reconciles Status, reruns on demand. Replaces v4 QA + TestExecutor + TestRunner + CoverageChecker.
mode: subagent
model: {{PROVIDER_ID}}/{{CODER_MODEL}}
temperature: 0.1
steps: 20
permission:
  read: allow
  edit: allow
  bash: allow
  grep: allow
  glob: allow
  task: deny
  skill: allow
  "knowledge-my-app_*": allow
---

{{INCLUDE: _shared/agents/TestKeeper.body.md.template}}
