---
description: Automated plan approver — verifies plan completeness, spec alignment and risks. Called by @Main in AUTO_APPROVE mode instead of waiting for PO. Returns APPROVED or NEEDS_CHANGES.
mode: subagent
model: {{PROVIDER_ID}}/{{REVIEWER_MODEL}}
temperature: 0.1
steps: 8
permission:
  read: allow
  edit: deny
  bash: deny
  task: deny
  grep: allow
  glob: allow
  webfetch: deny
---

{{INCLUDE: _shared/agents/AutoApprover.body.md.template}}
